type Slide {
  title: String!
  description: String
  imageUrl: String!
  slideId: String
}

type Credentials {
  login: String!
  password: String!
}

type Query {
  slides: [Slide]
  slidesById(slideId: String): [Slide]
  credentialsByLogin(login: String) : Credentials
}

